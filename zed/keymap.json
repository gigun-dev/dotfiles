[
  {
    "context": "Workspace",
    "bindings": {
      // Cursor-like keybindings (Cmd+J / Cmd+L → ctrl+j / ctrl+l via AHK Mac-like remap)
      "ctrl-j": "workspace::ToggleBottomDock",
      "ctrl-l": "agent::ToggleFocus"
    }
  }
]
